#include <memory>

struct Node{
  Node *p; // Parent
  Node *a; // Left child
  Node *b; // Right Child
  unsigned long long b; // Segment start
  unsigned long long e; // Segment end
  long long v; // Value of the node

  Node(Node *p, Node *a, Node *b, unsigned long long a, unsigned long long){

  }
}
